<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title or metadata.title or "My Automated Blog" }}</title>

    {# Construct the CSS path manually using eleventy.pathPrefix #}
    {# This assumes 'assets' is at the root of your output directory before prefixing #}
    <link rel="stylesheet" href="{{ eleventy.pathPrefix }}/assets/css/style.css">

    {# Placeholder for additional head elements from child templates (Like OG tags) #}
    {% block head_extra %}{% endblock %}
</head>
<body>
    <header>
        <nav>
            {# Construct the Home link path manually #}
            <a href="{{ eleventy.pathPrefix }}/">Home</a>
        </nav>
        <h1>{{ metadata.title or "My Automated Blog" }}</h1>
        <p>{{ metadata.description or "Welcome to my awesome blog!" }}</p>
    </header>
    <main>
        {{ content | safe }}
    </main>
    <footer>
        <p>&copy; {% year %} {{ metadata.author.name or "Your Name Here" }}. All rights reserved.</p>
    </footer>
    {# Placeholder for any scripts at the end of the body #}
    {% block scripts %}{% endblock %}
</body>
</html>
